# RO-Online-toolbox 開發鐵則

目標：**遊戲重開**或**小更新**（新增道具／地圖／技能、模組位移、函式搬家）
不准讓任何功能壞掉或算錯。允許壞的只有大更新改底層（函式重寫、結構欄位偏移改變）。
所有數值都要**準確**：不能猜、不能推理、不能用「看起來像」「編號應該連號」的值。

## 最高原則：位址一律 AOB 特徵掃描，不准寫死

**這個工具是要給大家用的。** 所以每一個功能都必須在下面三種情況下**照樣找得到**：

1. **換一台電腦**（別人的機器上沒有你的 `RODATA/`、沒有你的路徑、沒有你的檔案）
2. **遊戲改版**（模組位移、結構搬家）
3. **重開遊戲／重開機**（動態配置的位址每次都不一樣）

達成方式只有一種：**特徵搜尋**。用指令骨架或資料樣式當錨，把答案**當場搜出來**，
100% 確定位置。不准記位址、不准存上次的結果、不准「大概在那附近」。

**任何記憶體位址都不准硬編進程式碼。** 遊戲每次重開、每次小改版，動態配置的
位址就會變 —— 寫死的那一刻它就已經是壞的，而且壞得很安靜。

- 值的位址 → 用 AOB 特徵掃描（`services/aob.py`）定位。
- 函式／全域／vtable → 同樣用特徵定位，不准記絕對位址。
- 同一個位址**不准在第二個地方再寫一次**：位移後另一份不會跟上，也不會警告。
- 特徵裡不准把答案寫死：模組內的 4-byte 立即值、rel32 一律遮成 `??`，
  靠指令骨架當錨，不要靠剛好等於某個值的位元組。
- 定位失敗要**大聲**：功能停用並顯示警示。不准拿舊值、預設值或猜的值繼續算。

### 唯一允許寫死的兩類

- **結構偏移**（`+0xBC` 這種）—— 同一個結構內部的欄位距離，本來就屬於
  「大更新才會壞」的類別。但必須留出處註解：這個偏移是怎麼推出來的。
- **函式呼叫版面**（參數數量、呼叫慣例）—— 同上。

### ⛔ 不算結構偏移的東西（看起來很像，其實是地雷）

**「A 全域減 B 全域」的固定距離不是結構偏移，一律禁止。**
兩個獨立的全域在改版時位移幅度可以不一樣，那條推導就斷了 ——
而且斷得毫無徵兆：指到的地方通常是一片 0，讀出來是合法值。

實際踩過（GAMEDATA [MEM-039]）：角色座標寫成「HP 全域 − 0x3AD5FC」，
2026-08-26 改版時 HP 全域移了 `+0x60D8`、座標只移了 `+0x60B8`。
`read_position()` 開始回 `(0,0)`，而合理性檢查只擋 `>=512`，
所以 **(0,0) 通過了驗證**，自動打怪拿它當 A* 起點走去地圖角落，全程不報錯。

**正確做法：找程式碼引用做成 `CodeSignature`。**
[MEM-006] 當初說「座標前 0x30 bytes 全是 0，做不出樣式」—— 那是指**資料**樣式；
**程式碼**樣式沒這個問題，寫入端的指令骨架非常有辨識度（見 `POSITION_X_SIGS`）。
找不到引用才回頭考慮別的辦法，不要一看到「資料做不出樣式」就退回寫死。

### 特徵命中多個 ≠ 特徵壞了

兩種完全不同的原因，處理方式相反 —— **先把候選攤開來看再決定**：

- **特徵不夠精確** → 改特徵。
- **環境裡有長得像的雜訊** → 加**驗證**，不要動特徵。

實際踩過（[MEM-041]）：角色特徵命中 6 個，其中 5 個是堆積垃圾
（HP 15、maxHP 42 億、名字空白），真的角色也在裡面。舊版看到「不只一個」
就直接放棄，症狀是使用者回報「遊戲明明開著卻讀不到」。
**AOB 只是錨，分辨「這是不是我要的東西」要靠內容驗證。**
驗完還是不只一個，那才是真的分不出來 —— 那時仍然大聲失敗，不准賭。

### 資料檔也一樣：不准依賴只有開發機有的東西

`RODATA/`（解包出來的遊戲資源）**只有開發機有**，而且大到不可能打包
（光地形就 1800 MB）。任何執行時會用到的資料，一律用腳本抽成
`assets/*.gz` 隨程式一起走，並收進 `RO-Online-toolbox.spec`。

- 抽取腳本放 `tools/build_*.py`，要能重跑（改版後重抽）。
- 抽出來的東西要**壓到能打包**：地形只留「每格能不能站」1 bit，
  1082 張從 1800 MB 變 1.5 MB（[DAT-022]）。
- **不准用「檔案在不在」當判斷依據**。`gat_path(name).is_file()` 在使用者的
  電腦上永遠是 False，等於把功能全部拒絕掉。要問就問資料層（`has_terrain()`）。

## RO 掛 GameGuard：只讀不寫（血的教訓）

RO 的 Ragexe.exe 掛著 nProtect GameGuard，會偵測並反制對行程的寫入：

- **唯讀記憶體可以**（ReadProcessMemory 放行，角色狀態就是這樣讀的）。
- **寫記憶體／注入／改 IAT 一律禁止**——GameGuard 偵測到會讓遊戲當機或封號
  （見 GAMEDATA [PKT-011]、[PKT-013]，使用者實際踩過）。
- 要送動作走**封包**：RO 封包是明文（[PKT-012]），DUP_HANDLE 沒被剝，
  可複製遊戲 socket 送封包，全程不碰記憶體。
- 讀網路封包用 raw socket（outbound）或 Npcap（inbound），在網路層，
  GameGuard 看不到。

新功能一律照這個界線設計。injector.py 只適用於沒有 GameGuard 的遊戲。

## 資料來源優先序（動手前先走完這個判斷）

1. **執行時讀遊戲自己載進記憶體的資料**。改版自動跟上，零維護。
   要寫死任何遊戲資料之前，**先確認遊戲有沒有把那份資料載進記憶體**。
2. **AOB 定位的位址**（見上）。
3. **確認過真的不在記憶體裡，才准抄 `RODATA/` 解包資料**，而且：
   - 用腳本從資源檔**自動抽**，不准手打、不准從編號規律推。
   - 查不到／對不上 → **拒絕動作或安全退化**，絕不拿猜的值送給遊戲。
   - 每份抽出來的表要記錄它是對哪一版核對的，改版後重跑抽取腳本。

## 硬規則

- **ID 不照系列連號，不能猜。** 不確定一律留空 ——
  留空＝安全退化，填錯＝很有自信的錯。
- **存身分，不存位置。** 使用者的設定、記下來的目標、跨拍傳遞的東西，
  一律存**穩定的身分**（道具編號、角色 AID、怪物 GID），不准存**會挪動的位置**
  （背包格號、清單第幾列、螢幕座標）。要用的時候拿身分**當場查**現在的位置。
  格號會挪動：丟東西、賣東西、用完一整疊，後面的都會往前遞補（[MEM-028]）。
  存格號的那一刻沒有錯，錯的是三分鐘後 —— 而且它會**安靜地喝錯東西**。
  實作見 `services/potion.py` 的 `_slot_of()`：設定存編號，格號每次現查。
- **不准用「等幾秒」當作機制。** 每一步都要等一個**讀得到的訊號**：
  記憶體裡的值、封包、視窗狀態、畫面判定。禁止「睡 1.5 秒讓它穩定」這種寫法 ——
  機器慢一點就整個錯位，而且失敗時看不出是哪一步壞掉。
  正確的形狀是 **做 → 讀 → 確認 → 再往下**，讀不到就重做或大聲失敗。
  逾時只能當**放棄的上限**，不能當**成功的依據**。
  （例：打完字不要睡著等，去記憶體裡找那串字；找到才按送出。）

- **交給遊戲的位址／背包格號，送出前當場重讀重驗**，不能信先前那一拍讀到的
  （背包會在中間變動、實體會被回收）。
- 讀取端一律做合理性驗證（指標範圍、vtable 對得上、數值範圍、兩份欄位交叉比對），
  驗不過就退安全預設，不准拿垃圾值繼續算。
- 失效模式只允許兩種：**大聲停用**（警示＋功能不動）或**安全退化**（顯示編號／少做事）。
  「安靜地做錯事」（送錯格、算錯百分比）一律當 bug 修。

## 做完一個段落就自己 push，不要等人叫

一個段落 = 功能可用、測試全綠、事實已經寫進 `GAMEDATA.md` **與 memory**
（見下一節）。到這裡就**自己 commit 並 push 到 main**，不用問。累積一大坨再一次推，等於每個提交都變成
「好幾個功能混在一起、只有最終狀態驗過」，出事時沒辦法二分找回哪一步壞的。

push 之前一定要做的三件事：

1. **跑完整測試**（`pytest -q`）。沒綠不准推 —— 這個 repo 有自動更新，
   推上去的東西會直接換掉使用者手上的程式。
2. **看一遍 `git status` 的未追蹤檔案**，確認沒有夾帶不該進版控的東西。
   最常見的三類：封包擷取輸出（**RO 封包是明文，登入那包裡有帳號與認證
   位元組**）、`RODATA/` 解包資料、打包／診斷的暫存產物。
   新出現的這類東西要順手加進 `.gitignore`，不是這次避開就算了。
3. **提交訊息寫「為什麼」**，尤其是踩過的坑與被否決的做法 ——
   那是下一個 session 唯一讀得到的線索。
4. **確認過的事實寫進 `GAMEDATA.md` 並更新 memory**（見下一節）。

還在 debug、中間狀態、或功能只做一半，就**不要**推。那不叫段落。

## 事實記錄：**確認過的事情，兩個地方都要寫**

「確認過」= 實機量到、或從真的封包／資料解出來的。推論、猜測、「應該是」不算，
那種要標明**還沒驗證**。

1. **`GAMEDATA.md`** —— 完整的事實與怎麼驗的，含**走不通的路**。規範見該檔開頭。
   每找到一條 AOB 特徵要記成 `MEM` 條目，附**怎麼生成的**與**怎麼驗證唯一性**。
2. **`~/.claude/projects/.../memory/`** —— 給下一個 session 的接手筆記：
   現況到哪、待辦是什麼、哪些方向已經確定是死路。**一件事一個檔**，
   細節不要抄過去（那是 GAMEDATA 的工作），memory 只留「該去看哪裡」。

為什麼兩個都要：GAMEDATA 是查得到的帳本，但**沒人會沒事翻它**；
memory 是新對話一開始就會載入的索引。只寫 GAMEDATA，下一個 session
會把同一條死路再走一次。

順序：**改完 → 測試綠 → 寫 GAMEDATA → 更新 memory → push**。

## 現況

- **AOB 工具**：`services/aob.py`（`scan`／`locate_global`／`CodeSignature`）。
- **特徵登錄表**：`services/signatures.py`（已集中）。每條都寫了骨架出處、
  實機命中數與驗證方式。
- **驗證工具**：`tools/verify_sigs.py`。

各功能的定位方式：

| 位置 | 定位方式 |
|---|---|
| `services/character.py` | 角色狀態結構（資料樣式 ＋ **命中多個時用數值驗證挑出真的**） |
| `services/player_position.py` | 角色座標（**進圖座標全域**用程式碼骨架；**移動元件**用 `GID == AID` 當場掃出來）|
| `signatures.NAVI_DEST_SIGS` | 遊戲尋路的目標地圖（CRT 靜態建構鏈） |
| `signatures.SELECT_CURSOR_SIGS` / `SELECT_NAME_SIGS` | 選角畫面的游標與名字 |
| `services/bag.py` | 背包容器（除以 34 的魔術乘數 → 解析函式 → `mov ecx, imm32`） |
| `services/packet_table.py` | 封包長度表（`mov ecx,esi; call rel32` 最多人呼叫的那個） |

打包資產（使用者的電腦沒有 `RODATA/`，這些是唯一來源）：
`assets/` 的 `items` / `mobs` / `warps` / `mapnames` / `terrain` / `icons`，
產生腳本都在 `tools/build_*.py`。

### ⚠ 特徵找得到 ≠ 找到的是對的東西（2026-08-28，[MEM-047]）

舊的 `POSITION_X/Y_SIGS` 每一項技術指標都漂亮：實機 1 處命中、四個立即值
互相驗證、`y` 剛好 `x+4`、改版模擬也過。**但它指到的是小地圖的標記** ——
沒有小地圖圖檔的地圖（1082 張裡有 **396 張**，室內圖幾乎全中）上那段程式碼
根本不會執行，全域就停在上一張圖的座標，而 `position_located` 照樣是 True。

**新增特徵時要多做一件事：把命中處所在的函式看一遍**，確認它在做的事
就是你以為的那件事。做法很便宜 —— 把那個位址當位元組搜整個模組映像，
看有幾處引用（那條全域**全檔只有 2 處**，都在同一個畫小地圖的函式裡），
再反組譯一段看它在幹嘛。

**2026-08-26 已清掉的寫死位址**（留著當範例，同類問題照這個做）：
`StatusOffsets.position`、`input_helper.SUBMITTED_ACCOUNT_OFFSET`、
`input_helper.SHARED_INPUT_OFFSET`（後者實測**沒有任何程式碼引用得到**，
本來就定位不了，而且沒人在用 —— 直接刪掉，知識留在 `field_addresses` 的註解裡）。
