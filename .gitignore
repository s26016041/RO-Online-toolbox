__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
*.log
logs/
config.local.json

# RO 客戶端解包資料（24 萬檔案，只在本機分析用，不進版控）
RODATA/

# 診斷產出（verify_sigs、farm_test 等工具寫的報告與快照，只在本機看）
reports/

# OTP 綁定用的 QR 截圖 —— 等同種子明文，絕對不能進版控
qr.jpg
*-qr.png
*-qr.jpg

# 封包擷取的原始輸出 —— RO 封包是明文（[PKT-012]），登入那包裡有帳號與認證位元組
封包/
*.etl
PktMon.etl

# 打包實驗的殘留
onedir-test.spec

# Nuitka 的建置產物（387 MB）。留著只是為了記錄「這條路走過」，不進版控。
build_nuitka/
